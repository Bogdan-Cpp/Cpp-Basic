#include <iostream>
#include <vector>

int main(){

    system("pause");
    return 0;
}

vector<int> twoSum(vector<int>& nums, int target) {
    std::vector<int> lista;
    int curr = 0;
    int prev = 0;
}